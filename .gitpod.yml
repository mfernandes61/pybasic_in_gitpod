ports:
- port: 8888
  onOpen: open-browser
tasks:
#annoyingly despite perfectly good env vars existing we have to enter the home dir
- init: bash /workspace/pybasic_in_gitpod/run.sh
  command: cp -r /workspace/pybasic_in_gitpod/.jupyter /home/gitpod

